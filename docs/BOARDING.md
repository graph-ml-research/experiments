# 🔹 Настройка PyCharm и структура проекта для курса

Рекомендуется использовать **PyCharm** (Community или Professional). Настройка IDE и репозитория обеспечивает единый workflow и упрощает работу с git и виртуальными окружениями.

---

## 1. Структура папки исследователя

```
students/<yourname>/
├── experiments/ # Все экспериментальные наработки
│ ├── main_code/ # Основной проект / стабильный код
│ │ ├── model.py
│ │ ├── train.py
│ │ └── utils.py
│ └── scratch/ # Быстрые прототипы и эксперименты
│ ├── lstm_try.py
│ └── cnn_experiment.py
├── progress.md # Еженедельные отчёты
└── README.md
```

**Принцип работы:**

1. **main_code/**  
   - Стабильный код проекта.
   - Используется как база для финального проекта и инкубатора.
   - Комментарии и docstrings допустимы на русском, желательно дублировать на английском.

2. **scratch/**  
   - Любые быстрые эксперименты или «грязный» код.
   - Все файлы должны быть в `.py`.
   - Комментарии можно писать на русском или английском.
   - Здесь можно тестировать идеи без риска повредить основной код.

3. **progress.md**  
   - Обновляется еженедельно.
   - Содержит ссылки на конкретные файлы и эксперименты.
   - Пример:
     ```markdown
     ## Неделя 1
     Эксперименты:
     - `experiments/scratch/lstm_try.py`
     - `experiments/main_code/model.py` (базовая структура)
     ```

---

## 2. Настройка PyCharm

1. **Открываем проект:** `File → Open` → папка `people/<yourname>`.
2. **Python Interpreter:**
   - Создаём виртуальное окружение (venv) внутри папки проекта.
   - Убедиться, что выбран для проекта.
3. **Установка зависимостей:**
```
pip install -r ../../shared/requirements.txt
```

Git интеграция:

File → Settings → Version Control → Git

Настроить remote origin (GitHub)

Push идёт на оба репозитория. 

## 3. Git workflow
Всегда pull перед началом работы:
```
git pull origin main
```
Работаем в своей директории:
- Основной код: experiments/main_code/
- Быстрые эксперименты: experiments/scratch/

Коммиты небольшими шагами с информативными сообщениями:
```
git add experiments/main_code/model.py
git commit -m "Add initial LSTM architecture"
git push
```

Обновление прогресса:
students/<yourname>/progress.md

Для крупных экспериментов можно создавать ветки git:
```
git checkout -b lstm_experiment
```

## 4. Политика по Jupyter
Jupyter Notebook не используется для экспериментов и основного кода.

Все эксперименты должны быть в .py.

Notebook можно использовать только локально для визуализации графиков, но эти файлы не коммитятся в main_code или инкубатор.

Основной код (main_code/) и инкубатор должны быть полностью в .py.

## 5. Использование других IDE
Разрешается под свою ответственность.

Обязательные требования:
- Соблюдать структуру проекта
- Следовать языковой политике (русский/английский)
- Поддерживать git workflow

Преподаватель поддерживает только PyCharm.

## 6. Рекомендации по workflow
- Основной код всегда находится в main_code/.
- Все эксперименты и наброски — в scratch/.
- Обновление progress.md каждый понедельник.
- Push на GitHub.

Когда эксперимент стабилизируется → переносим его в main_code/ для инкубатора.

## 7. Полезные ссылки

<a href="https://www.jetbrains.com/pycharm/documentation/">PyCharm Docs</a>

<a href="https://www.jetbrains.com/help/pycharm/using-git-integration.html">Git integration in PyCharm</a>
